services:
  mail-backup:
    image: ghcr.io/issogr/mail-backup:latest
    restart: unless-stopped
    environment:
      PUID: "${PUID:-1000}"
      PGID: "${PGID:-1000}"
      TZ: "${TZ:-UTC}"
    volumes:
      - ./vol:/vol
